
server:
  port: 8080
  servlet:
    context-path: /employees-service
  error:
    include-message: always

spring:
  application:
    name: employees-service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/employees_astralife_test
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
